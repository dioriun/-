<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_guangbo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_guangbo">
        <activity android:name=".student_add_Activity"></activity>
        <activity android:name=".student_update_Activity" />
        <activity android:name=".student_Activity" />
        <activity android:name=".admin_denglu_Activity" />
        <activity android:name=".admin_student_update_Activity" />
        <activity android:name=".admin_student_add_Activity" />
        <activity android:name=".admin_student_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>